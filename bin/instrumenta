#!/usr/bin/env node
const { exec } = require("child_process")
const nodePath = "."
const child = exec(`npm run cli ${process.argv.slice(2)}`, {
  env: {
    NODE_PATH: nodePath,
  },
})

child.stdout.pipe(process.stdout)
child.stderr.pipe(process.stderr)
